# Changelog

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **设备类型标签** — 连接设备即"心率设备"（心率 + 信号双重判断），监听设备即"普通设备"（仅信号判断），设置页显示当前模式
- 心率设备解锁规则 — 心率与信号**同时正常**才解锁（连续 3 次心率采样 + 信号强于恢复阈值），任一缺失即锁屏
- 配置校验 — 已配置设备但类型缺失/非法时自动清空设备配置，提示重新选择设备

### 修复

- 重连后偶发只监听信号、未恢复心率的问题 — 连接过程内部清理不再误报断开（避免连接被反复打断）；设备名为空时连接失败不再卡死重连状态机；连接建立后随即断开的事件竞态不再错误停止重连
- 应用启动时自动连接已配置设备获取心率（设备不在线则进入广播监听，出现后自动重连）；普通设备仅监听信号、不自动连接，避免无心率服务导致连接失败循环

## [1.0.1] - 2026-08-05

### 新增

- **白天/黑夜主题切换** — 头部开关一键切换，主题选择自动记忆，默认暗色；设备表格、输入框、开关、滑块、弹窗等 Element Plus 组件随主题统一适配
- **蓝牙未启动提示** — 扫描设备、启动 RSSI 监听、自动重连时若蓝牙功能关闭，弹窗提示"检测到蓝牙未开启"，并在设置页设备列表空态与仪表盘 RSSI 区域持续显示

### 修复

- 重启软件后设备不在场时不再误锁屏 — 仅当设备从"有信号/已连接"变为"无信号/断开"时才触发锁屏
- 设备表格背景不符合暗色主题（白底）的问题
- `npm run typecheck` 在 TypeScript 5.9 下崩溃（vue-tsc 升级至 2.2.12）

### 其他

- 增大启动窗口初始尺寸（960×800），仪表盘显示无滚动条
- 升级 Electron 至 28.3.3
- 设置页补充说明：解锁账户名无需填写，自动使用当前 Windows 登录账户
- 开发约定：npm 安装命令统一使用 npmmirror 镜像

## [1.0.0] - 2026-07-31

首个正式版本：基于 BLE 心率监测的 Windows 自动锁屏/解锁工具。

### 新增

- **BLE 心率监测** — 连接标准 BLE 心率设备（智能手表、心率带），实时显示心率
- **自动锁屏** — 设备断开或心跳超时（默认 15 秒，可配置）时自动锁定 Windows
- **自动解锁** — 心率恢复后通过 Windows Credential Provider 原生认证自动解锁，替代不可靠的 SendKeys 模拟按键方案
- **自动重连** — 设备意外断开后自动启动广播监听并重连；用户手动断开则不会重连
- **系统托盘** — 最小化到托盘后台运行
- **可配置项** — 心跳超时时间、解锁确认时长（1–10 秒，默认 3）、自动解锁开关、开机自启动
- **密码安全存储** — Windows DPAPI 加密登录密码，仅当前用户可解密
- **锁屏检测** — koffi FFI 调用 Win32 `OpenInputDesktop` 实时检测锁屏状态
- **RSSI 信号监听模式** — 通过蓝牙广播信号强度判断设备距离，信号弱持续锁屏、信号恢复自动解锁；可独立监听，也可与心率连接并行（连接时心率 + RSSI 规则同时生效）
- **解锁组件可选安装/卸载** — 设置页可安装/卸载 UnlockProvider.dll 凭据提供程序（UAC 提权注册到 System32 与 HKLM），未安装时自动禁用解锁相关功能
- **自定义应用图标** — 窗口、系统托盘、exe 统一应用应用图标

### 修复

- 打包后无法运行的问题：koffi 原生模块正确纳入打包、`ble-helper` 改为自包含发布、构建前自动结束旧进程
- 构建流程改用 `--dir` 免签名模式（NSIS 安装包需联网下载 winCodeSign）
- BLE 扫描 watcher 构造后状态为 Created 导致 `Start()` 未执行的问题
- 重连期间连接失败被误判为用户手动断开的问题

### 其他

- 新增构建说明文档（`build:dir` 与 `build:nsis` 区分）
- 打包配置：本地复用已安装的 Electron 二进制避免联网下载失败，新增 npmmirror 下载镜像，启用 exe 图标编辑（`signAndEditExecutable`），新增 sharp 图标转换依赖
- 暂时隐藏 afterPack 清理流程（`scripts/after-pack.js` 保留，便于恢复）

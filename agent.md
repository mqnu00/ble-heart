# Agent 规则

本文件定义 AI 助手在本仓库中的行为约束,所有工作必须遵守。

## 提交与推送权限(最高优先级)

- **git commit、git push、tag 创建/删除/推送、GitHub Release 发布等任何版本控制写操作,必须事先获得项目所有者(用户)的明确允许。**
- 允许的信号:用户明确说出「提交」「推送」「发布」「重新发布」「整理修改并提交」「commit」「push」等指令。
- 用户未明确授权时,Agent 只能:
  - 修改工作区文件(编辑、创建、删除本地文件)
  - 运行本地构建、测试、类型检查等只读/本地命令
- 一次授权只覆盖当次请求,不得延伸为后续操作的默认许可。
- 发现已执行但未获授权的提交/推送,应立即报告并停止后续写操作。

## 工作约定

- 使用中文回复。
- 修改前先阅读目标文件,不修改无关代码,不做超出请求范围的改动。
- 保持最小改动,遵循仓库现有代码风格与结构。
- 涉及外部可见操作(推送、发布、删除远程资源)一律先询问用户。
